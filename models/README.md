# Models

Store user-managed local model files here. The reset scripts preserve this entire directory.

DeepSeek Harness uses cloud APIs by default; selecting a local model endpoint remains a Harness configuration choice.
